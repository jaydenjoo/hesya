/**
 * Mock fixtures barrel — Sprint 2A/2C/2D에서 페이지별 fixture를 점진 추가.
 *
 * 사용:
 *   import { mockTrendingStores } from "@/lib/mock-fixtures";
 *   const data = env.MOCK_FIXTURES ? mockTrendingStores : await getRealTrending(db);
 */

export * from "./_types";
